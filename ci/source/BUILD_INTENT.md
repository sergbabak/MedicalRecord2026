Native validation target for RC 0.4.1:
- Windows x64 Development installer (.exe): build, silent install, application --self-test, uninstall.
- macOS Apple Silicon Development installers (.pkg/.dmg): build, install, application --self-test, bundle/DMG verification.

Production signing/notarization remains fail-closed and is not represented by these Development artifacts.
